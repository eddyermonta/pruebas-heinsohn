package punto2;

import java.util.Collections;
import java.util.Arrays;

public class NumeroMayor {
    public int numeroMasAlto(Integer[] numbers) {
        return Collections.max(Arrays.asList(numbers));
    }
}
